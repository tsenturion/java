/**
 * Демонстрационный класс для обучения отладке (debug) в Java
 * Показывает процесс пошагового выполнения кода и анализа переменных
 */
public class DebugDemo {
    public static void main(String[] args) {
        System.out.println("🚀 Начало выполнения программы");
        
        // Инициализация переменных - хорошее место для первой точки останова
        int a = 5;
        int b = 3;
        
        System.out.println("a = " + a + ", b = " + b);
        
        // Вызов метода add - можно поставить точку останова на этой строке
        int sum = add(a, b);
        System.out.println("Сумма a + b = " + sum);
        
        // Вызов метода multiplyByTwo - еще одна возможная точка останова
        int result = multiplyByTwo(sum);
        System.out.println("Результат умножения суммы на 2 = " + result);
        
        // Финальный результат
        System.out.println("🎯 Итоговый результат: " + result);
        
        // Дополнительные примеры для отладки
        debugComplexExample();
    }

    /**
     * Метод сложения двух чисел
     * Хорошее место для изучения работы с параметрами и возвращаемыми значениями
     */
    public static int add(int x, int y) {
        System.out.println("📥 Вход в метод add: x = " + x + ", y = " + y);
        
        // Выполняем сложение - точка останова здесь покажет процесс вычисления
        int result = x + y;
        
        System.out.println("📤 Выход из метода add: результат = " + result);
        return result;
    }

    /**
     * Метод умножения числа на 2
     * Демонстрирует преобразование данных
     */
    public static int multiplyByTwo(int number) {
        System.out.println("📥 Вход в метод multiplyByTwo: number = " + number);
        
        // Умножаем на 2 - можно наблюдать за изменением значения
        int result = number * 2;
        
        System.out.println("📤 Выход из метода multiplyByTwo: результат = " + result);
        return result;
    }

    /**
     * Более сложный пример для отладки с циклами и условиями
     */
    public static void debugComplexExample() {
        System.out.println("\n🔍 Начало сложного примера для отладки");
        
        int[] numbers = {1, 2, 3, 4, 5};
        int total = 0;
        
        // Цикл for - можно отлаживать пошагово, наблюдая за изменением i и total
        for (int i = 0; i < numbers.length; i++) {
            System.out.println("🔄 Итерация цикла: i = " + i + ", numbers[i] = " + numbers[i]);
            
            // Условная логика - хорошее место для точки останова
            if (numbers[i] % 2 == 0) {
                System.out.println("   ✅ Четное число: " + numbers[i]);
                total += numbers[i] * 2; // Четные числа умножаем на 2
            } else {
                System.out.println("   ❌ Нечетное число: " + numbers[i]);
                total += numbers[i]; // Нечетные числа добавляем как есть
            }
            
            System.out.println("   📊 Текущий total = " + total);
        }
        
        System.out.println("🎯 Финальный результат сложного примера: " + total);
        
        // Рекурсивный пример
        int factorialResult = factorial(5);
        System.out.println("Факториал 5 = " + factorialResult);
    }

    /**
     * Рекурсивный метод для демонстрации отладки вызовов стека
     */
    public static int factorial(int n) {
        // Базовый случай - остановка рекурсии
        if (n <= 1) {
            System.out.println("🏁 Базовый случай: factorial(" + n + ") = 1");
            return 1;
        }
        
        // Рекурсивный вызов - можно наблюдать стек вызовов
        System.out.println("📞 Рекурсивный вызов: factorial(" + n + ") = " + n + " * factorial(" + (n-1) + ")");
        int result = n * factorial(n - 1);
        
        System.out.println("📤 Возврат из factorial(" + n + ") = " + result);
        return result;
    }
}