/*
Создайте класс Book с полями: title, author, year, isAvailable (доступна ли
для выдачи).
Создайте класс Library, который хранит массив книг.
Реализуйте методы:
• addBook(Book book) — добавить книгу.
• borrowBook(String title) — взять книгу (если доступна).
• returnBook(String title) — вернуть книгу.
• listAvailableBooks() — вывести доступные книги.
*/

public class Book {
    
}
